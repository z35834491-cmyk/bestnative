# 业务架构蓝图

首页「全局总览」读这个文件，**不是**拓扑图。

改这里就能调整：
- 分层（接入 / 业务 / 平台 / 中间件）
- 每个组件名称、说明
- 关键链路（下单、发布等）
- 基础设施说明

Shore 会自动把 `components[].name` 和 K8s 发现的服务名对上，显示健康状态（绿/黄/红）。

部署后改完 JSON，刷新首页即可，不用重启（有缓存的话重启 API）。
