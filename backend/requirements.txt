# ============================================================
# BestNative Backend — requirements.txt
# Python 3.11+  |  AI-Native 运维指挥中心
# ============================================================

# ---- Web Framework ----
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ---- Database (PostgreSQL + pgvector) ----
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
pgvector>=0.3.0

# ---- Validation / Config ----
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ---- Cache / PubSub ----
redis>=5.0.0

# ---- AI / LLM / Agent ----
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
openai>=1.50.0

# ---- Local Embedding / Rerank (RAG) ----
# 注：sentence-transformers 依赖 torch（~2GB），移至 requirements-ml.txt
# RAG 功能懒加载，未安装时自动降级（不影响核心 API/发现/发布/安全扫描）
# 需启用 RAG 时：pip install -r requirements-ml.txt

# ---- Observability clients ----
elasticsearch[async]>=8.0.0
prometheus-api-client>=0.5.5

# ---- Infra Providers ----
kubernetes>=30.1.0
boto3>=1.34.0
paramiko>=3.4.0

# ---- Auth / Security ----
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# ---- HTTP / Utils ----
httpx>=0.27.0
aiofiles>=24.0.0
python-dotenv>=1.0.0
structlog>=24.0.0
tenacity>=8.0.0
websockets>=13.0
apscheduler>=3.10.0
